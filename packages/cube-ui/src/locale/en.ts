import type { Locale } from '.';

const localeValues: Locale = {
    Common: {
        Rreset: 'Reset',
        Confirm: 'Confirm',
    },
    Demo: {
        test: '12313',
    },
};

export default localeValues;
